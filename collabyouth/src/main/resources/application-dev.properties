# ============================================================
#  application-dev.properties — configuration locale (Docker)
# ============================================================

# ── Base de données locale (Docker) ──────────────────────────
spring.datasource.url=jdbc:postgresql://localhost:5432/collabyouth
spring.datasource.username=collabyouth_user
spring.datasource.password=collabyouth_pass
spring.datasource.driver-class-name=org.postgresql.Driver

# ── JWT ──────────────────────────────────────────────────────
jwt.secret=dev_secret_key_collabyouth_2025_change_in_prod
jwt.expiration=86400000
