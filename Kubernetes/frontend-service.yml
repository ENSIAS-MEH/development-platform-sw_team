apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: collabyouth-prod
spec:
  selector:
    app: frontend
  type: ClusterIP  # ✅ CHANGÉ
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      # ❌ nodePort: SUPPRIMÉ